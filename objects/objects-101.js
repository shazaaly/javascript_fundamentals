let myBook = {
    title: '1984',
    auther: 'George Orwell',
    pageCount: 326
}

console.log(`Book data: Auther is ${myBook.auther} - Title : ${myBook.title} and page count : ${myBook.pageCount}`)

myBook.title = `Animal Farm`
console.log(`Book title will be : ${myBook.title} `)

//challenge area

student = {
    fname: 'Andrew',
    age: 23,
    location: 'Philadelphia'
}

console.log(`${student.fname} is ${student.age} and lives in ${student.location}`)

student.age = student.age + 1
console.log(`${student.fname} is ${student.age} and lives in ${student.location}`)


