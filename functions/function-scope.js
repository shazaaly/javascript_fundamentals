
//Global scope (convert,temp1, temp2)
    // local scope : fah ,celesius argument 
            // local : isFreezing

let convert = function (fahrenheit) {
    let celesius = (fahrenheit - 32) * 5 / 9
    if (celesius <= 0) {
        let isFreezing = true
    } 

}

let fName = 'Shaza'
console.log(`Hey my name is: ${fName} and my age is 22` )
